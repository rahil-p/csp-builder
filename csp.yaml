Content-Security-Policy:
  default-src:
    - "'none'"
  script-src:
    - "'self'"
  connect-src:
    - "'self'"
    - https://${API_DOMAIN}:${API_PORT}
  font-src:
    - "'self'"
    - https://my-site.com
  base-uri:
    - "'self'"
  report-to:
    - ${CSP_REPORT_URI}